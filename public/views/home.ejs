<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Home</title>
</head>
<body>
    <div class="container m-3 p-2">
        <h1 id="welcome">Bienvenido al sitio</h1>
        <div class="container d-flex justify-content-end my-2" id="loginBox">
            <div class="d-flex flex-column align-items-center">
                <img src=/images/<%= foto %> style="width:3rem;"></img>
                <h4><%= nombre %></h4>
                <button type="button" class="btn btn-primary" id="logoffButton" onclick="location.href='/api/logout'">Log off</button>
            </div>
        </div>

        <div class="container d-flex bg-light my-2"></div>
            <h4>Carrito: <%= cartId %></h4>
            <%cart.forEach(function(item){%>
            <div class="container d-flex bg-light my-2" id="cartBox">
                <h6 class="m-2">Título: <%= item.nombre %> </h6>
                <h6 class="m-2">Precio: <%= item.precio %></h6>
                <img class="m-2" src=<%=item.foto %> style="width:3rem;"></img>
            </div>
            <%})%>

        <h2>Product list</h2>
       <div class="container d-flex">
            <% allProducts.forEach(function(product) { %>
            <div class="card m-2" style="width: 14rem;">
                <img src=<%= product.foto %> class="card-img-top" style="width: 14rem; height:18rem;">
                    <div class="card-body">
                        <h5 class="card-title"><%= product.nombre %></h5>
                        <h6 class="card-subtitle mb-2 text-muted"><%= product.precio %></h6>
                        <p class="card-text"><%= product.descripcion %></p>
                        <a href="#" class="btn btn-primary">Comprar</a>
                    </div>
                </div>
            <% }); %>
        </div> 
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</body>
</html>