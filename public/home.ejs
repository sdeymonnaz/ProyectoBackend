<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Home</title>
</head>
<body>
    <h1>HOME</h1>
    <div class="container">
        <h1 id="welcome">Bienvenido</h1>
        <div class="container d-flex my-2" id="loginBox">
          <h3>Usuario: <%= user.username %></h3>
          <h3>Nombre: <%= user.nombre %></h3>
          <img src=/images/<%= user.foto %> style="width:3rem;"></img>
        </div>
        <h5>Carrito: <%= user.cart %></h5>
        <button type="button" class="btn btn-primary my-2" id="logoffButton" onclick="location.href='/api/logout'">Log off</button>

        <h2>Product list</h2>
        <div class="container d-flex">
            <% products.forEach(function(product) { %>
            <div class="card m-2" style="width: 18rem;">
                <img src=<%= product.foto %> class="card-img-top" style="height:18rem;" alt="...">
                    <div class="card-body">
                        <h5 class="card-title"><%= product.nombre %></h5>
                        <h6 class="card-subtitle mb-2 text-muted"><%= product.precio %></h6>
                        <p class="card-text"><%= product.descripcion %></p>
                        <a href="#" class="btn btn-primary">Comprar</a>
                    </div>
                </div>
            <% }); %>
<!--             <table class="table table-striped m-2">
                <thead>
                        <tr>
                            <th>ID</th>
                            <th>Nombre</th>
                            <th>Descripcion</th>
                            <th>Precio</th>
                            <th>Cover</th>
                        </tr>
                </thead>
                <tbody>
                    <% products.forEach(function(product) { %>
                        <tr>
                            <td><%= product.id %></td>
                            <td><%= product.nombre %></td>
                            <td><%= product.descripcion %></td>
                            <td><%= product.precio %></td>
                            <td><img width="200" src="<%= product.foto %>" alt=""></img></td>
                        </tr>
                    <% }); %>
                </tbody>    
            </table> -->
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</body>
</html>